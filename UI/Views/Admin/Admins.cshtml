@model IEnumerable<ApplicationUser>

@{
    ViewData["Title"] = "Admins";
}

<div class="container mt-5">
    <partial name="_AlertsPartial" />

    <table class="table table-sm table-hover">
        <theader>
            <tr>
                <th class="text-bold">Email</th>
                <th class="text-bold text-center">Action</th>
            </tr>
        </theader>
        <tbody>
            @if (Model?.Count() > 0)
            {
                foreach (var admin in Model.OrderBy(u=> u.Email))
                {
                    <tr>
                        <td>@admin.Email</td>
                        <td style="width: 80px;">
                            <a class="btn btn-sm btn-danger" asp-controller="Admin" asp-action="DeleteAdmin" asp-route-adminId="@admin.Id" asp-route-email="@admin.Email">Delete</a>
                        </td>
                    </tr>
                }
            }
            <tr class="tr-placeholder">
                <td colspan="2" class="text-center">No admins</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-sm btn-success" asp-controller="Admin" asp-action="AddAdmin">Add admin</a>
</div>
