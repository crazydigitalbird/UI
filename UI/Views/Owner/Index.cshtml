@model IEnumerable<Agency>

<div class="container">

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Message"]
            <button type="button" class="btn-close" data-mdb-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-mdb-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <table class="table table-sm table-hover">
        <thead>
            <tr>
                <th class="text-bold">Name</th>
                <th class="text-bold text-center" class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model?.Count() > 0)
            {
                foreach (var agency in Model)
                {
                    <tr>
                        <td class="col-sm-8">@agency.Name</td>
                        <td class="col-sm-4 text-center">
                            <a class="btn btn-sm btn-primary" asp-controller="Owner" asp-action="EditAgency" asp-route-id="@agency.Id">Edit</a>
                            <a class="btn btn-sm btn-danger" asp-controller="Owner" asp-action="DeleteAgency" asp-route-id="@agency.Id">Delete</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td class="text-center" colspan="2">No Data</td>
                </tr>
            }
        </tbody>
    </table>

    <a class="btn btn-sm btn-success" asp-controller="Owner" asp-action="AddAgency">Add Agency</a>

</div>

